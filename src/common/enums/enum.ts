export enum AUTH_PATH {
  LOGIN = 'login',
  REGISTRATION = 'registration',
  FORGOT_PASSWORD = 'forgotPassword',
  CREATE_NEW_PASSWORD = 'createNewPassword',
}
export enum ADMIN_PATH {
  MAIN = 'main' /* for page dashboard */,
  EMPLOYEES = 'employees',
  PRICES = 'prices',
  REPORTS = 'reports',
}
